[package]
name = "tauri-app"
version = "0.1.0"
edition = "2021"

[dependencies]
tauri = { version = "1.3", features = ["api-all"] }
ironvault-core = { path = "../../crates/ironvault-core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0"

[package.metadata.tauri]
productName = "IRONVAULT"
