[package]
name = "ironvault-core"
version = "0.1.0"
edition = "2021"

[lib]
name = "ironvault_core"
path = "src/lib.rs"

[dependencies]
crypto = { path = "../crypto" }
db = { path = "../db" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0"
rand = "0.8"

